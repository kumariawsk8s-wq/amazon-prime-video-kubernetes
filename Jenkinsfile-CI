pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo "Cloning Repository..."

                git branch: 'main',
                    url: 'https://github.com/kumariawsk8s-wq/amazon-prime-video-kubernetes.git'
            }
        }
    }
}
